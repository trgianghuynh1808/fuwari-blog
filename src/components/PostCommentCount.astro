---
import CommentCount from "./CommentCount.svelte";

export interface Props {
  postSlug: string;
  isListPage?: boolean;
}

const { postSlug, isListPage = false } = Astro.props;
---

<CommentCount postSlug={postSlug} isListPage={isListPage} client:load />
